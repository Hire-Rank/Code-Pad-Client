import { Button } from '@/components/ui/button';
import { useEffect } from 'react'

const Auth = () => {
    useEffect(() => {

    });

    return (
        <div>
        <Button>Click</Button>
        </div>
    )
}

export default Auth
